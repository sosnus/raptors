export class MapTransformation {
    Id;
    x: number;
    y: number;
    yaw: number;


    constructor(Id, x: number, y: number, yaw: number) {
      this.Id = Id;
      this.x = x;
      this.y = y;
      this.yaw = yaw;
    }
}
